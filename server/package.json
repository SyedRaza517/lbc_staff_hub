{
  "name": "lbc-staff-hub-server",
  "version": "1.0.0",
  "description": "London Brookes College Staff Hub — API server",
  "main": "src/index.js",
  "scripts": {
    "dev": "node --watch --watch-path=./src src/index.js",
    "start": "node src/index.js",
    "migrate": "prisma migrate dev --name init",
    "generate": "prisma generate",
    "seed": "node prisma/seed.js",
    "studio": "prisma studio",
    "setup": "prisma generate && prisma migrate dev --name init && node prisma/seed.js",
    "postinstall": "prisma generate"
  },
  "prisma": {
    "seed": "node prisma/seed.js"
  },
  "dependencies": {
    "@prisma/client": "^5.22.0",
    "bcryptjs": "^2.4.3",
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "express": "^4.21.2",
    "jsonwebtoken": "^9.0.2",
    "nodemailer": "^9.0.3",
    "prisma": "^5.22.0"
  }
}
